# Шаблон портфолио для карточек товара

Автоматизированная система для создания портфолио проектов с карточками товара для Behance.

## 🚀 Быстрый старт

1. Откройте `index.html` в браузере
2. Откройте консоль разработчика (F12)
3. Используйте доступные команды для управления

## 📁 Структура проекта

```
template/
├── index.html          # Основной HTML файл
├── styles.css          # Стили
├── script.js           # JavaScript логика
└── README.md           # Документация

assets/
├── backgrounds/        # Фоновые изображения проектов
│   └── project-name-bg.jpg
├── cards/              # Карточки товара
│   ├── card-001.jpg
│   ├── card-002.jpg
│   └── ...
└── icons/              # Декоративные иконки (40px или 80px)
    ├── icon-camera.svg
    ├── icon-palette.svg
    └── ...              # Иконки выбираются случайно и размещаются группами
```

## 🎨 Использование

### Подготовка проекта

1. **Фон**: Поместите два фоновых изображения в `assets/backgrounds/`:
   - `project-name-bg.jpg` - версия А (оригинальная)
   - `project-name-bg-B.jpg` - версия Б (вертикально зеркальная)
   - Оба файла должны иметь одинаковую высоту (рекомендуется мин. 3000px)
   - Фон скроллится вместе с контентом и автоматически чередуется: А → Б → А → Б...
   - Фон загружается только до конца подвала (не бесконечно)

2. **Карточки**: Настройте пути к папкам с карточками в `script.js`:
   - Укажите базовый путь: `CONFIG.cardsBasePath = 'Z:/!_STUFF/ALTAIR/КАРТОЧКИ/'`
   - Укажите папки для каждой группы: `CONFIG.cardsGroupFolders = ['папка1', 'папка2', ...]`
   - Каждая группа загружает карточки из своей папки
   - Первая карточка определяется автоматически: ищется с "_01_" и "_sht" в имени
   - Крупные карточки (на 2 колонки): автоматически выбираются из не первых
     - Если карточек > 11 в группе → 2 крупных
     - Если карточек ≤ 11 → 1 крупная

3. **Иконки**: Добавьте декоративные иконки в `assets/icons/`:
   - Размеры: строго 40px или 80px (задаются автоматически)
   - Формат: SVG, PNG, JPG
   - Иконки размещаются группами (3, 5-7, 4-9 штук) в разных местах макета
   - Выбор иконок и их размещение - случайные

4. **Настройка**: Измените `CONFIG.projectName` и пути в `script.js`

### Редактирование контента

- **Тексты**: Кликните на текстовые блоки для редактирования (contenteditable)
- **Карточки**: Используйте drag-and-drop или кнопки управления
- **Группы**: Настройте количество карточек в `CONFIG.cardsPerGroup`

### Переключение формата карточек

Шаблон поддерживает два формата карточек с возможностью мгновенного переключения:

#### Через панель управления (правый верхний угол)
- Кнопка **3:4 Portrait** - переключить на вертикальный формат
- Кнопка **1:1 Square** - переключить на квадратный формат

#### Через консоль
```javascript
handleFormatChange('portrait')  // Переключить на 3:4
handleFormatChange('square')    // Переключить на 1:1
```

#### Через конфигурацию
В `script.js` измените значение по умолчанию:
```javascript
const CONFIG = {
    // ...
    cardFormat: 'square' // 'portrait' или 'square'
};
```

**Важно**: Формат применяется ко всем карточкам одновременно. Смешанные форматы в одном макете не поддерживаются.

### Управление карточками

#### Drag & Drop
Перетаскивайте карточки мышью для обмена местами между любыми позициями и группами.

#### Кнопки управления (появляются при наведении)
- **★** - Сделать карточку первой в группе (займет 2 колонки)
- **⇄** - Режим swap: кликните на две карточки для обмена местами

#### Консольные команды

```javascript
toggleDevMode()              // Включить/выключить визуализацию сетки
exportState()                // Показать текущее состояние в JSON
resetLayout()                // Сбросить раскладку к начальному состоянию
handleFormatChange('square') // Переключить на формат 1:1
handleFormatChange('portrait') // Переключить на формат 3:4
exportToPDF()                // Экспорт в PDF (window.print)
exportToBehance()            // Экспорт для Behance (в разработке)
```

#### Горячие клавиши
- `Ctrl/Cmd + D` - Включить/выключить Dev Mode
- `Escape` - Отменить режим swap

## 📐 Технические параметры

- **Ширина макета**: 1440px (фиксированная)
- **Колонки**: 4 колонки с равномерным распределением
- **Боковые отступы**: 40px
- **Отступ между группами**: 100px
- **Пропорции карточек**: 3:4 (portrait) или 1:1 (square) - переключаемо
- **Первая карточка группы**: 2 колонки (двойная ширина) - определяется по "_01_" и "_sht"
- **Крупные карточки**: автоматически выбираются из не первых (1-2 штуки в зависимости от количества)
- **Иконки**: группы по 3, 5-7, 4-9 штук, размеры строго 40px или 80px

### Форматы карточек

Шаблон поддерживает два формата карточек, которые можно переключать в любой момент:

#### 3:4 Portrait (вертикальные)
- Соотношение сторон: 3:4
- Используется для: вертикальных карточек товара, упаковок, стоящих предметов
- По умолчанию активен этот формат

#### 1:1 Square (квадратные)
- Соотношение сторон: 1:1
- Используется для: квадратных карточек, логотипов, иконок, симметричных композиций
- Переключается через панель управления или консоль

## 🔧 Настройка

### Настройка путей к карточкам

Отредактируйте в `script.js`:

```javascript
const CONFIG = {
    // Базовый путь к папке с карточками
    cardsBasePath: 'Z:/!_STUFF/ALTAIR/КАРТОЧКИ/',
    
    // Папки для каждой группы (относительно cardsBasePath)
    cardsGroupFolders: [
        'КОННЕКТОР_910000101',  // Группа 1
        'КОННЕКТОР_910000102',  // Группа 2
        'КОННЕКТОР_910000103',  // Группа 3
        'КОННЕКТОР_910000104'   // Группа 4
    ],
    // ...
};
```

**Правила именования карточек:**
- Первая карточка: должна содержать "_01_" и "_sht" в имени (например: `card_01_sht.jpg`)
- Если нет карточки с "_01_", берется первая найденная в папке
- Система автоматически ищет файлы по паттернам: `1.jpg`, `01.jpg`, `001.jpg`, `_1_.jpg`, `_01_.jpg`

### Изменение фона

```javascript
const CONFIG = {
    // ...
    projectName: 'your-project-name' // Будет искать your-project-name-bg.jpg
};
```

### Настройка цветов и размеров

Измените CSS переменные в `styles.css`:

```css
:root {
    --layout-width: 1440px;
    --side-padding: 40px;
    --group-gap: 100px;
    --card-gap: 20px;
    --icon-size: 70px;
    /* ... */
}
```

## 📤 Экспорт

### PDF
1. Нажмите `Ctrl/Cmd + P` или вызовите `exportToPDF()` в консоли
2. Настройте параметры печати:
   - Размер: Custom (1440px ширина)
   - Масштаб: 100%
   - Поля: Нет
3. Сохраните как PDF

### Behance (секции 1440×900px)
*Функция в разработке*

Будет автоматически разрезать макет на секции 1440×900px с бесшовной стыковкой.

## 🐛 Отладка

### Dev Mode
Включите через `toggleDevMode()` или `Ctrl/Cmd + D` для визуализации:
- Красные границы - контейнеры групп
- Зеленые границы - обычные карточки
- Голубые границы - первые карточки групп

### Проверка состояния
```javascript
exportState() // Выведет полную структуру данных в консоль
```

### Сброс
```javascript
resetLayout() // Перезагрузит карточки с начальной конфигурацией
```

## 📋 Чеклист перед экспортом

- [ ] Все тексты проверены и отредактированы
- [ ] Карточки расположены оптимально
- [ ] Фон дублируется корректно
- [ ] Иконки не перекрывают важный контент
- [ ] Проверка на разных масштабах (zoom 50%, 100%, 150%)
- [ ] Dev mode выключен
- [ ] Консоль не показывает ошибок

## 🎯 Структура макета (4 раздела)

1. **Вступление** - Приветственный текст + обзорная графика
2. **Процесс работы** - Описание этапов работы над проектом
3. **Галерея карточек** - Основной контент (4 группы по 8-15 карточек)
4. **Подвал** - Скриншот маркетплейса + контакты

## 🔄 Workflow

1. **Создание** → Настройте CONFIG, загрузите ресурсы
2. **Наполнение** → Отредактируйте тексты, расставьте карточки
3. **Проверка** → Dev mode, разные масштабы
4. **Экспорт** → PDF или секции для Behance

## 💡 Советы

- Используйте WebP для веб-превью, оригиналы для PDF
- Первая карточка группы - самая важная/красивая
- Декоративные иконки: 2-3 на каждые 900px высоты
- **Фон**: Версия Б должна быть точным вертикальным зеркалом версии А для бесшовного перехода
- Фон автоматически подгружается по мере скролла до конца контента
- Сохраняйте резервные копии перед большими изменениями
- **Формат 3:4** лучше для вертикальных товаров (бутылки, упаковки, стоящие предметы)
- **Формат 1:1** лучше для логотипов, иконок, квадратных композиций
- Переключайте формат в процессе работы, чтобы выбрать оптимальный вариант
- Все карточки в макете должны быть одного формата для единообразия

### Работа с фоном

**Структура файлов:**
- `project-name-bg.jpg` - версия А (оригинальная)
- `project-name-bg-B.jpg` - версия Б (зеркальная)

**Логика чередования:**
- Фон скроллится вместе с контентом (не fixed)
- Автоматическое чередование: А → Б → А → Б → ...
- Новые сегменты подгружаются автоматически при приближении к концу текущего фона
- Если версия Б отсутствует, используется только версия А

**Создание зеркальной версии:**
В Photoshop/редакторе:
1. Откройте `project-name-bg.jpg`
2. Image → Image Rotation → Flip Canvas Vertical
3. Сохраните как `project-name-bg-B.jpg`

## 🆘 Поддержка

При возникновении проблем:
1. Откройте консоль разработчика (F12)
2. Проверьте ошибки
3. Используйте `exportState()` для диагностики
4. Попробуйте `resetLayout()`

---

**Версия**: 1.0.0  
**Дата**: 02.01.2026

# 📋 ПОЛНАЯ ДОКУМЕНТАЦИЯ ПРОЕКТА ПОРТФОЛИО

## 🎯 ОБЩЕЕ ОПИСАНИЕ

Интерактивный шаблон портфолио для создания и управления множественными проектами с карточками товаров. Проект представляет собой полнофункциональную систему управления портфолио с возможностью создания, редактирования, сохранения и экспорта проектов.

**Технологии:** HTML5, CSS3, Vanilla JavaScript (ES6+), LocalStorage API

**Размер макета:** 1440px фиксированная ширина

**Целевая платформа:** Desktop (Windows), веб-браузеры

---

## 📁 СТРУКТУРА ПРОЕКТА

```
PORTFOLIO/
├── assets/
│   ├── backgrounds/          # Фоновые изображения
│   │   ├── project-name-bg.jpg
│   │   ├── project-name-bg-B.jpg
│   │   ├── BACK_TEXTURE_*.png
│   └── icons/
│       ├── PNG_BLACK/        # PNG иконки (всегда используются)
│       │   └── ICON_BLACK_0*.png
│       ├── SVG_BLACK/        # SVG иконки для светлой темы
│       │   └── ICON_SVG_BLACK_0*.svg
│       └── SVG_WHITE/        # SVG иконки для темной темы
│           └── ICON_SVG_WHITE_0*.svg
├── template/
│   ├── index.html            # Основная страница шаблона
│   ├── script.js             # Основная логика (2000+ строк)
│   ├── styles.css            # Все стили (1100+ строк)
│   └── README.md             # Базовая документация
├── console.html              # Консоль управления проектами
└── SHABLON_BEHANCE_1440/
    ├── PROCESS_Images/       # Изображения для секции процесса
    └── OZON_PAGE/
        └── OZON IMAGE.jpg    # Скриншот маркетплейса
```

---

## 🎨 ОСНОВНЫЕ КОМПОНЕНТЫ И ФУНКЦИОНАЛ

### 1. СИСТЕМА УПРАВЛЕНИЯ ПРОЕКТАМИ

#### 1.1 Консоль управления (console.html)
- **Назначение:** Центральная страница для управления всеми проектами портфолио
- **Функции:**
  - Отображение списка всех сохраненных проектов
  - Создание нового проекта
  - Открытие существующего проекта
  - Переименование проекта
  - Дублирование проекта
  - Удаление проекта
  - Экспорт всех проектов

#### 1.2 Сохранение/Загрузка проектов
- **Хранение:** LocalStorage, ключ `portfolioProjects`
- **Структура данных проекта:**
  ```javascript
  {
    id: string,                    // Уникальный ID проекта
    name: string,                  // Название проекта
    lastModified: ISO string,      // Дата последнего изменения
    groups: Array,                  // Массив групп карточек
    cardSettings: Object,          // Настройки карточек (рамки, тени)
    backgroundSettings: Object,    // Настройки фона
    iconSettings: Object,          // Настройки иконок
    featuredCards: Array,          // Избранные карточки
    processImage: string,          // Путь к изображению процесса
    config: Object                 // Конфигурация проекта
  }
  ```

#### 1.3 URL параметры
- `?project=new` - создание нового проекта
- `?project={id}` - загрузка проекта по ID

---

### 2. СИСТЕМА ГРУПП КАРТОЧЕК

#### 2.1 Структура группы
```javascript
{
  id: number,                      // ID группы (1, 2, 3...)
  name: string,                    // Название группы (редактируемое)
  cards: Array,                    // Массив карточек
  format: 'portrait' | 'square'    // Формат карточек в группе
}
```

#### 2.2 Структура карточки
```javascript
{
  id: string,                      // Уникальный ID (groupId-cardIndex)
  src: string,                     // Путь к изображению
  alt: string,                     // Альтернативный текст
  groupId: number,                  // ID группы
  isFirst: boolean,                // Главная карточка группы
  isLarge: boolean,                 // Большая карточка (2 колонки)
  positionInGroup: number,         // Позиция в группе
  isEmpty: boolean                 // Пустой блок-разделитель
}
```

#### 2.3 Правила расположения карточек в группах
- **Первая карточка:** Всегда большая (занимает колонки 1-2)
- **Следующие две:** Маленькие (колонки 3-4)
- **Следующая большая:** Колонки 3-4, слева две маленькие
- **4 маленькие:** Могут образовать блок 2x2 или встать в ряд
- **Расстояние:** Равно расстоянию между колонками
- **Выравнивание:** По верху или низу больших карточек

#### 2.4 Форматы карточек
- **Portrait (3:4):** Вертикальные карточки (по умолчанию)
- **Square (1:1):** Квадратные карточки

---

### 3. СИСТЕМА ФОНОВ

#### 3.1 Типы фонов
- **Изображение:** Фоновое изображение из папки `assets/backgrounds/`
- **Цвет:** Сплошной цвет
- **Градиент:** CSS градиент

#### 3.2 Логика загрузки фона
1. Проверка `state.backgroundSettings.imagePath` (пользовательский фон)
2. Если нет - загрузка по имени проекта: `{projectName}-bg.jpg`
3. Если есть `{projectName}-bg-B.jpg` - загрузка как второй сегмент
4. Если нет `-B.jpg` - дублирование первого фона как второй сегмент
5. Фон всегда центрирован по 1440px
6. Сегменты идут друг за другом вертикально

#### 3.3 Параметры фона
- **Размер:** 1440px ширина, пропорциональная высота
- **Позиционирование:** Центр по горизонтали, начало с верха
- **Дублирование:** Автоматическое после первого сегмента
- **Скроллинг:** Параллакс эффект (скорость 0.5x)

---

### 4. СИСТЕМА ИКОНОК

#### 4.1 Структура папок иконок
- **PNG_BLACK:** PNG формат, используются всегда (на любом фоне)
  - Имена: `ICON_BLACK_001.png`, `ICON_BLACK_002.png`...
- **SVG_BLACK:** SVG формат, только для светлой темы
  - Имена: `ICON_SVG_BLACK_001.svg`, `ICON_SVG_BLACK_002.svg`...
- **SVG_WHITE:** SVG формат, только для темной темы
  - Имена: `ICON_SVG_WHITE_001.svg`, `ICON_SVG_WHITE_002.svg`...

#### 4.2 Логика размещения иконок
- Иконки размещаются **вокруг** текстовых блоков, не перекрывая их
- **Отступ от текста:** 20px минимум
- **Размещение:** По краям секций (слева, справа, сверху, снизу)
- **Z-index:** Иконки выше фона (z-index: 1), но ниже контента (z-index: 3)

#### 4.3 Типы рандомизации
- **По позиции:** Рандомное размещение иконок в зонах размещения
- **По именам файлов:** Перемешивание порядка загрузки иконок

---

### 5. СИСТЕМА НАСТРОЕК КАРТОЧЕК

#### 5.1 Рамки карточек
- **Толщина:** 0px, 1px, 2px, 3px (предустановки)
- **Цвет:** Пользовательский выбор + предустановки (черный, белый, серый)
- **Применение:** Ко всем карточкам проекта одновременно
- **Исключение:** Пустые блоки не имеют рамки

#### 5.2 Тени карточек
- **Включение/выключение:** Чекбокс
- **Предустановки:**
  - Слабая: `0 2px 8px rgba(0, 0, 0, 0.2)`
  - Средняя: `0 4px 20px rgba(0, 0, 0, 0.3)`
  - Сильная: `0 8px 40px rgba(0, 0, 0, 0.5)`
  - Резкая: `0 0 20px rgba(0, 0, 0, 0.6)`

#### 5.3 Глобальные настройки
- Кнопка редактирования (⚙️) в правом верхнем углу (непечатаемая)
- Модальное окно с настройками
- Применение ко всем карточкам проекта

---

### 6. СЕКЦИИ СТРАНИЦЫ

#### 6.1 Секция "Вступление" (section--intro)
- **Заголовок:** Редактируемый (`contenteditable`)
- **Текст:** Редактируемый параграф
- **Featured Cards:** Первые 4 главные карточки из групп (горизонтально)
- **Фон текста:** Такой же стиль как в секции процесса
- **Иконки:** Декоративные иконки вокруг текста

#### 6.2 Секция "Процесс работы" (section--process)
- **Заголовок:** "Процесс работы"
- **Изображение процесса:** 
  - Загружается из `SHABLON_BEHANCE_1440/PROCESS_Images/`
  - Ширина: 1360px, пропорциональная высота
  - Размещение: Над блоками текста, 4 колонки ширины
- **Блоки процесса:**
  1. Фотография и обработка
  2. Контент и тексты
  3. Концепция и дизайн
- **Параметры блоков:**
  - Высота: 356px (фиксированная)
  - Текст: Выравнивание по верху, padding-top: 20px
  - Стрелки между блоками: Видимые, показывают последовательность
- **Иконки:** Декоративные иконки вокруг секции

#### 6.3 Секция "Галерея" (section--gallery)
- **Заголовок:** "Карточки товара"
- **Группы карточек:** Динамически генерируемые группы
- **Между группами:** Декоративные иконки
- **Заголовок группы:** Редактируемое поле ввода (вместо "Группа 1")
- **Кнопка редактирования:** Открывает модальное окно редактирования

#### 6.4 Секция "Результат" (section--footer)
- **Заголовок:** "Результат"
- **Скриншот маркетплейса:**
  - Изображение: `SHABLON_BEHANCE_1440/OZON_PAGE/OZON IMAGE.jpg`
  - Размещение: Над контактами, 4 колонки ширины
  - Возможность перемещения стрелками (по 1px)
  - Сохранение позиции в localStorage
- **Карточки на скриншоте:**
  - 4 окна: 255x335px каждое
  - Отступ слева: 304px
  - Отступ справа: 82px
  - Расстояние между окнами: 11.5px
  - Размещение: Первые карточки первых 4 групп
  - Для квадратных карточек (1:1): Центрирование в окне
  - Возможность перемещения мышкой
- **Контакты:** Редактируемый блок с контактной информацией
- **Иконки:** Декоративные иконки вокруг секции

---

### 7. МОДАЛЬНЫЕ ОКНА

#### 7.1 Окно редактирования группы (edit-group-modal)
- **Размер:** На весь экран (100vw x 100vh)
- **Структура:**
  - **Левая часть (2/3):** Предпросмотр группы в масштабе 25%
  - **Правая часть (1/3):** Элементы управления
- **Функции:**
  - Просмотр всех карточек группы
  - Перемещение карточек drag & drop
  - Изменение размера карточки (1x/2x)
  - Назначение главной карточки (★)
  - Добавление пустых блоков
  - Удаление карточек
  - Добавление новых файлов
  - Удаление группы

#### 7.2 Окно добавления группы (add-group-modal)
- Выбор формата карточек (Portrait/Square)
- Указание пути к папке с карточками
- Или ручной ввод списка файлов

#### 7.3 Окно настроек карточек (card-settings-modal)
- Настройка рамок (толщина, цвет)
- Настройка теней (включение, предустановки)

#### 7.4 Окно настроек фона (background-settings-modal)
- Выбор типа фона (изображение/цвет/градиент)
- Рандомайзер фонов
- Загрузка нового фона

#### 7.5 Окно выбора изображения процесса (process-image-modal)
- Список доступных изображений из папки PROCESS_Images
- Предпросмотр изображений
- Выбор изображения

---

### 8. ПАНЕЛЬ УПРАВЛЕНИЯ

#### 8.1 Верхняя панель (format-control)
- Переключение формата карточек (Portrait/Square)
- Добавление новой группы
- Настройки карточек (⚙️ Карточки)
- Настройки фона (🖼️ Фон)
- Выбор изображения процесса (🖼️ Процесс)
- Рандомизация иконок (🎲 Иконки)
- Переход в консоль (📁 Консоль)

#### 8.2 Кнопка быстрого редактирования (card-editor-controls)
- Расположение: Правый верхний угол
- Непечатаемая (скрывается при печати)
- Открывает окно настроек карточек

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ РЕАЛИЗАЦИИ

### 1. STATE MANAGEMENT

#### 1.1 Глобальный объект state
```javascript
const state = {
  cards: [],                    // Все карточки
  groups: [],                   // Группы карточек
  draggedCard: null,            // Текущая перетаскиваемая карточка
  dropTarget: null,            // Целевой элемент для drop
  backgroundSegments: [],       // Сегменты фона
  backgroundHeight: 0,         // Общая высота фона
  currentSegmentIndex: 0,      // Индекс текущего сегмента
  icons: {                     // Загруженные иконки
    pngBlack: [],
    svgBlack: [],
    svgWhite: []
  },
  projectId: null,             // ID текущего проекта
  projectName: string,          // Название проекта
  cardSettings: {              // Настройки карточек
    borderWidth: number,
    borderColor: string,
    shadowEnabled: boolean,
    shadowPreset: string
  },
  backgroundSettings: {        // Настройки фона
    type: 'image' | 'color' | 'gradient',
    imagePath: string | null,
    color: string,
    gradient: string,
    mirror: boolean
  },
  iconSettings: {             // Настройки иконок
    lightThemeGroup: 'SVG_BLACK',
    darkThemeGroup: 'SVG_WHITE',
    randomized: boolean,
    randomizeType: 'position' | 'filename'
  },
  featuredCards: [],          // Избранные карточки
  processImage: string | null, // Изображение процесса
  processImages: [],          // Список доступных изображений
  currentEditGroupId: number | null // ID редактируемой группы
};
```

### 2. КЛЮЧЕВЫЕ ФУНКЦИИ

#### 2.1 Инициализация
- `init()` - Главная функция инициализации
- `loadIcons()` - Загрузка иконок из папок
- `loadBackground()` - Загрузка фона
- `loadCards()` - Загрузка карточек из папок
- `loadProcessImages()` - Загрузка списка изображений процесса
- `setupIcons()` - Размещение иконок на странице
- `setupEventListeners()` - Настройка обработчиков событий
- `setupControls()` - Настройка панели управления
- `applyCardSettings()` - Применение настроек карточек
- `renderMarketplaceCards()` - Рендеринг карточек на скриншоте

#### 2.2 Работа с группами
- `renderAllGroups()` - Рендеринг всех групп
- `renderGroup(group)` - Рендеринг одной группы
- `createCardElement(cardData, format)` - Создание элемента карточки
- `handleAddGroup()` - Добавление новой группы
- `handleEditGroup(groupId)` - Открытие окна редактирования группы
- `reorderCardsInGroup()` - Переупорядочивание карточек в группе
- `updateGroupName(groupId, newName)` - Обновление названия группы

#### 2.3 Работа с фоном
- `loadBackgroundSegment(imagePath, isA)` - Загрузка сегмента фона
- `loadBackgroundSegmentMirrored(imagePath)` - Дублирование фона
- `updateBackgroundHeight()` - Обновление высоты контейнера фона
- `setupBackgroundScrollListener()` - Настройка параллакс эффекта
- `randomizeBackground()` - Рандомизация фона
- `applyBackgroundSettings()` - Применение настроек фона

#### 2.4 Работа с иконками
- `setupIcons()` - Размещение иконок с проверкой пересечений
- `randomizeIcons(type)` - Рандомизация иконок (position/filename)

#### 2.5 Работа с проектами
- `saveProject()` - Сохранение проекта в localStorage
- `loadProject(projectId)` - Загрузка проекта из localStorage

#### 2.6 Рендеринг специальных секций
- `renderFeaturedCards()` - Рендеринг избранных карточек
- `renderMarketplaceCards()` - Рендеринг карточек на скриншоте
- `setupMarketplaceScreenshotControls()` - Настройка перемещения скриншота

### 3. CSS СТРУКТУРА

#### 3.1 CSS Custom Properties
```css
:root {
  --layout-width: 1440px;
  --side-padding: 40px;
  --group-gap: 100px;
  --card-gap: 20px;
  --color-text: #ffffff;
  --color-text-secondary: rgba(255, 255, 255, 0.8);
  /* ... */
}
```

#### 3.2 Z-index слои
- **Фон:** z-index: -1
- **Иконки:** z-index: 1
- **Контент:** z-index: 3
- **Заголовки секций:** z-index: 3

#### 3.3 Grid система
- **Основной контейнер:** 4 колонки
- **Карточки:** Grid с автоматическим размещением
- **Featured cards:** Горизонтальная сетка (до 4 карточек)

---

## 📝 ДЕТАЛЬНЫЕ ТРЕБОВАНИЯ К РЕАЛИЗАЦИИ

### ТРЕБОВАНИЕ 1: Система управления проектами

**Задача:** Создать систему сохранения и загрузки множественных проектов портфолио.

**Подзадачи:**
1. Создать страницу консоли (`console.html`)
2. Реализовать функции сохранения проекта в localStorage
3. Реализовать функции загрузки проекта из localStorage
4. Создать UI для списка проектов с карточками
5. Реализовать функции: создание, открытие, переименование, дублирование, удаление
6. Отображать в карточке проекта: название, количество групп, дату изменения, список групп
7. Показывать фон проекта в карточке (как background-image)
8. Текст поверх фона с полупрозрачным фоном для читаемости

**Критерии успеха:**
- Все проекты сохраняются и загружаются корректно
- Консоль отображает все проекты с их фонами
- Все операции (создание, редактирование, удаление) работают без ошибок

---

### ТРЕБОВАНИЕ 2: Система групп карточек

**Задача:** Создать систему управления группами карточек с возможностью редактирования.

**Подзадачи:**
1. Создать структуру данных для групп и карточек
2. Реализовать загрузку карточек из папок по путям
3. Реализовать рендеринг групп с правильным расположением карточек
4. Создать правила расположения карточек (первая большая, затем маленькие и т.д.)
5. Реализовать drag & drop для переупорядочивания карточек
6. Добавить возможность изменения размера карточки (1x/2x)
7. Добавить возможность назначения главной карточки группы
8. Добавить возможность добавления пустых блоков-разделителей
9. Реализовать редактируемое название группы (вместо "Группа 1")
10. Поддержка двух форматов: Portrait (3:4) и Square (1:1)

**Критерии успеха:**
- Карточки правильно располагаются согласно правилам
- Drag & drop работает плавно
- Все функции редактирования работают корректно
- Названия групп сохраняются и отображаются

---

### ТРЕБОВАНИЕ 3: Система фонов

**Задача:** Создать систему управления фонами с автоматическим дублированием.

**Подзадачи:**
1. Создать контейнер для фона с фиксированной шириной 1440px
2. Реализовать центрирование фона (left: 50%, transform: translateX(-50%))
3. Реализовать загрузку первого сегмента фона
4. Реализовать автоматическое дублирование фона после первого сегмента
5. Реализовать параллакс эффект при скролле (скорость 0.5x)
6. Обеспечить, что фон всегда остается по центру при скролле
7. Поддержка пользовательских фонов, фонов по имени проекта, дефолтных фонов
8. Реализовать рандомайзер фонов
9. Поддержка типов фона: изображение, цвет, градиент

**Критерии успеха:**
- Фон всегда центрирован (не смещается при скролле)
- Фон автоматически дублируется после первого сегмента
- Параллакс эффект работает плавно
- Все типы фонов работают корректно

---

### ТРЕБОВАНИЕ 4: Система иконок

**Задача:** Создать систему декоративных иконок с умным размещением.

**Подзадачи:**
1. Реализовать загрузку иконок из трех папок (PNG_BLACK, SVG_BLACK, SVG_WHITE)
2. Реализовать выбор группы иконок в зависимости от темы (светлая/темная)
3. Реализовать размещение иконок вокруг текстовых блоков
4. Реализовать проверку пересечений с текстовыми блоками (отступ 20px)
5. Реализовать два типа рандомизации: по позиции и по именам файлов
6. Установить правильные z-index слои (иконки выше фона, ниже контента)
7. Обеспечить непрозрачность иконок (opacity: 1)

**Критерии успеха:**
- Иконки не перекрывают текстовые блоки
- Иконки правильно выбираются в зависимости от темы
- Рандомизация работает в обоих режимах
- Иконки всегда видны (не прозрачные)

---

### ТРЕБОВАНИЕ 5: Система настроек карточек

**Задача:** Создать систему глобальных настроек для всех карточек проекта.

**Подзадачи:**
1. Создать модальное окно настроек карточек
2. Реализовать настройку толщины рамки (0px, 1px, 2px, 3px)
3. Реализовать настройку цвета рамки (пользовательский + предустановки)
4. Реализовать включение/выключение теней
5. Реализовать предустановки теней (слабая, средняя, сильная, резкая)
6. Реализовать применение настроек ко всем карточкам проекта
7. Исключить пустые блоки из применения рамок
8. Создать кнопку быстрого доступа (правый верхний угол, непечатаемая)
9. Сохранять настройки в проекте

**Критерии успеха:**
- Все настройки применяются ко всем карточкам
- Пустые блоки не имеют рамок
- Настройки сохраняются и загружаются с проектом
- UI интуитивно понятен

---

### ТРЕБОВАНИЕ 6: Секция "Вступление"

**Задача:** Создать секцию вступления с избранными карточками.

**Подзадачи:**
1. Создать структуру секции с заголовком и текстом
2. Реализовать редактируемость текста (contenteditable)
3. Реализовать рендеринг избранных карточек (первые карточки из групп)
4. Ограничить количество избранных карточек до 4
5. Разместить карточки горизонтально
6. Учесть формат карточек (1:1 остается 1:1 в избранных)
7. Применить стиль фона текста как в секции процесса
8. Разместить декоративные иконки вокруг текста

**Критерии успеха:**
- Текст редактируется корректно
- Избранные карточки отображаются правильно
- Формат карточек сохраняется
- Иконки не перекрывают текст

---

### ТРЕБОВАНИЕ 7: Секция "Процесс работы"

**Задача:** Создать секцию процесса работы с изображением и блоками.

**Подзадачи:**
1. Создать структуру секции с заголовком
2. Реализовать загрузку изображения процесса из папки PROCESS_Images
3. Установить ширину изображения 1360px, пропорциональную высоту
4. Разместить изображение над блоками текста, на 4 колонки
5. Создать три блока процесса в правильном порядке:
   - Фотография и обработка
   - Контент и тексты
   - Концепция и дизайн
6. Установить фиксированную высоту блоков 356px
7. Выровнять текст по верху с padding-top: 20px
8. Создать видимые стрелки между блоками
9. Реализовать модальное окно выбора изображения процесса
10. Разместить декоративные иконки вокруг секции

**Критерии успеха:**
- Изображение процесса загружается и отображается корректно
- Блоки имеют одинаковую высоту
- Стрелки видны и показывают последовательность
- Можно выбрать изображение из списка

---

### ТРЕБОВАНИЕ 8: Секция "Галерея"

**Задача:** Создать секцию галереи с группами карточек.

**Подзадачи:**
1. Создать структуру секции с заголовком
2. Реализовать рендеринг всех групп карточек
3. Создать заголовок группы с редактируемым полем ввода
4. Реализовать кнопку редактирования группы
5. Разместить декоративные иконки между группами
6. Применить правильные правила расположения карточек
7. Реализовать компактное расположение карточек (без лишних пробелов)

**Критерии успеха:**
- Все группы отображаются корректно
- Названия групп редактируются и сохраняются
- Карточки располагаются согласно правилам
- Иконки размещены между группами

---

### ТРЕБОВАНИЕ 9: Секция "Результат" (маркетплейс)

**Задача:** Создать секцию результата со скриншотом маркетплейса и карточками.

**Подзадачи:**
1. Создать структуру секции с заголовком
2. Загрузить скриншот маркетплейса (OZON IMAGE.jpg)
3. Разместить скриншот над контактами, на 4 колонки
4. Реализовать возможность перемещения скриншота стрелками (по 1px)
5. Сохранять позицию скриншота в localStorage
6. Создать 4 окна для карточек: 255x335px каждое
7. Установить отступы: слева 304px, справа 82px, между окнами 11.5px
8. Разместить первые карточки первых 4 групп в окна
9. Для квадратных карточек (1:1): центрировать в окне
10. Реализовать возможность перемещения карточек мышкой
11. Сохранять позиции карточек в localStorage
12. Создать секцию контактов с редактируемым текстом
13. Разместить декоративные иконки вокруг секции

**Критерии успеха:**
- Скриншот можно перемещать стрелками
- Карточки размещаются в правильных окнах
- Квадратные карточки центрируются
- Карточки можно перемещать мышкой
- Позиции сохраняются

---

### ТРЕБОВАНИЕ 10: Модальное окно редактирования группы

**Задача:** Создать полноэкранное модальное окно для редактирования группы.

**Подзадачи:**
1. Создать модальное окно на весь экран (100vw x 100vh)
2. Разделить окно на две части:
   - Левая (2/3): Предпросмотр группы в масштабе 25%
   - Правая (1/3): Элементы управления
3. Реализовать предпросмотр группы с правильным масштабированием
4. Создать список карточек с drag & drop для переупорядочивания
5. Добавить кнопки управления для каждой карточки:
   - Изменение размера (1x/2x)
   - Назначение главной (★)
   - Удаление
6. Добавить кнопку добавления пустого блока
7. Добавить текстовое поле для добавления новых файлов
8. Добавить кнопки: удалить группу, отмена, сохранить
9. Обновлять предпросмотр при изменениях

**Критерии успеха:**
- Окно занимает весь экран
- Предпросмотр показывает группу в масштабе 25%
- Все функции редактирования работают
- Изменения применяются корректно

---

### ТРЕБОВАНИЕ 11: Дополнительные функции

**Подзадачи:**
1. Создать фавиконку из иконок проекта
2. Реализовать функцию экспорта в PDF (window.print())
3. Реализовать функцию экспорта для Behance (заглушка)
4. Добавить режим разработчика (Dev Mode) с горячей клавишей Ctrl+D
5. Реализовать консольные команды для отладки
6. Добавить стили для печати (скрытие UI элементов)
7. Реализовать обработку ошибок загрузки файлов
8. Добавить логирование в консоль для отладки

**Критерии успеха:**
- Все дополнительные функции работают
- Ошибки обрабатываются корректно
- Логирование помогает в отладке

---

## 🎯 ROADMAP РАЗРАБОТКИ

См. файл `ROADMAP.md` для детального плана разработки с чекбоксами.

---

## 📌 ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Все пути к файлам:** Использовать относительные пути от корня проекта
2. **LocalStorage:** Все данные сохраняются в браузере, очистка кеша удалит проекты
3. **Размер макета:** Фиксированная ширина 1440px, адаптивность не требуется
4. **Браузеры:** Проект разработан для современных браузеров (Chrome, Firefox, Edge)
5. **Файловая система:** Использование file:// протокола для локальных файлов
6. **Z-index:** Критически важно соблюдать правильные слои для корректного отображения
7. **Производительность:** При большом количестве карточек может потребоваться оптимизация

---

## 🔍 ТЕСТИРОВАНИЕ

### Чек-лист функциональности:
- [ ] Создание нового проекта
- [ ] Сохранение проекта
- [ ] Загрузка проекта
- [ ] Переименование проекта
- [ ] Дублирование проекта
- [ ] Удаление проекта
- [ ] Добавление группы
- [ ] Редактирование группы
- [ ] Удаление группы
- [ ] Перемещение карточек в группе
- [ ] Изменение размера карточек
- [ ] Назначение главной карточки
- [ ] Добавление пустых блоков
- [ ] Редактирование названия группы
- [ ] Настройки рамок карточек
- [ ] Настройки теней карточек
- [ ] Загрузка фона
- [ ] Рандомизация фона
- [ ] Дублирование фона
- [ ] Размещение иконок
- [ ] Рандомизация иконок
- [ ] Выбор изображения процесса
- [ ] Перемещение скриншота маркетплейса
- [ ] Перемещение карточек на скриншоте
- [ ] Редактирование текстов (contenteditable)
- [ ] Экспорт в PDF

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- Структура папок с иконками должна быть точно такой же, как описано
- Имена файлов иконок должны соответствовать паттернам
- Фоновые изображения должны быть в формате JPG или PNG
- Изображения процесса должны быть в папке PROCESS_Images
- Скриншот маркетплейса должен быть в формате JPG

---

**Версия документации:** 1.0  
**Дата создания:** 2025  
**Последнее обновление:** 2025
